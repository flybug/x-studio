import { XAutofocusDirective } from './x-autofocus.directive';

describe('XAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new XAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
