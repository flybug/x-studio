import { XAnchorDirective } from './x-anchor.directive';

describe('XAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new XAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
